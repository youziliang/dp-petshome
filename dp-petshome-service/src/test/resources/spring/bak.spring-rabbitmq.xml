<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p" xmlns:rabbit="http://www.springframework.org/schema/rabbit"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
	http://www.springframework.org/schema/beans/spring-beans.xsd
   http://www.springframework.org/schema/rabbit 
  http://www.springframework.org/schema/rabbit/spring-rabbit-1.5.xsd">

	<!-- addresses="${host:Port1},${host:Port2},${host:Port3} RQ集群配置参考 -->
	<rabbit:connection-factory id="connectionFactory" host="${amqp.riskcontrol.host}" port="${amqp.riskcontrol.port}" username="${amqp.riskcontrol.username}" password="${amqp.riskcontrol.password}" requested-heartbeat="60"
		virtual-host="${amqp.riskcontrol.vhost}" channel-cache-size="50" />

	<!--增加事物管理channel-transacted="true" -->
	<rabbit:template id="amqpTemplate" connection-factory="connectionFactory" channel-transacted="true" exchange="logs" routing-key="foo.steven" reply-timeout="30000" />

	<rabbit:admin connection-factory="connectionFactory" />

	<rabbit:queue name="testQueue" />

	<rabbit:topic-exchange name="logs">
		<rabbit:bindings>
			<rabbit:binding queue="testQueue" pattern="foo.#" />
		</rabbit:bindings>
	</rabbit:topic-exchange>

	<!-- mq listener配置 -->
	<bean id="fooListener" class="com.pingan.haofang.riskcontrol.service.rqlistener.FooMqListener" />

	<!-- 监听ack改为手动确认 acknowledge="manual" -->
	<rabbit:listener-container connection-factory="connectionFactory" acknowledge="manual">
		<rabbit:listener ref="fooListener" queue-names="testQueue" />
	</rabbit:listener-container>

</beans>